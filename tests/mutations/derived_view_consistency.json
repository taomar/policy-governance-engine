[
  {
    "name": "the projection re-derives its effect instead of restating the record's",
    "file": "src/policy_platform/infrastructure/projection/xacml_projection.py",
    "find": "    if record_effect is not _UNSET:",
    "replace": "    if False:",
    "tests": ["tests/unit/test_derived_views_do_not_contradict.py"]
  },
  {
    "name": "the published facts are not reconciled against required_facts",
    "file": "src/policy_platform/infrastructure/policy_facts.py",
    "find": "    facts = facts_for(rule)\n    if not required:\n        return facts",
    "replace": "    facts = facts_for(rule)\n    if required or not required:\n        return facts",
    "tests": ["tests/unit/test_derived_views_do_not_contradict.py"]
  },
  {
    "name": "provenance ignores the tree it is describing",
    "file": "src/policy_platform/infrastructure/formulation_mapping.py",
    "find": "        and (condition is None or not _is_vacuous(condition))",
    "replace": "        and True",
    "tests": [
      "tests/unit/test_derived_views_do_not_contradict.py",
      "tests/unit/test_stated_bound_end_to_end.py"
    ]
  },
  {
    "name": "the projection's compilation status is served again",
    "file": "src/policy_platform/contracts/xacml_projection.py",
    "find": "    compilation_status: CompilationStatus = Field(\n        default=CompilationStatus.NOT_EXECUTABLE, exclude=True\n    )",
    "replace": "    compilation_status: CompilationStatus = CompilationStatus.NOT_EXECUTABLE",
    "tests": ["tests/unit/test_derived_views_do_not_contradict.py"]
  },
  {
    "name": "the effect prose is served again",
    "file": "src/policy_platform/contracts/xacml_projection.py",
    "find": "    effect_basis: str = Field(default=\"\", exclude=True)",
    "replace": "    effect_basis: str = \"\"",
    "tests": ["tests/unit/test_derived_views_do_not_contradict.py"]
  }
]
