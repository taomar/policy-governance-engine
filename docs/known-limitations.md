# Known limitations

This page is for operators, reviewers and public readers deciding whether this build is safe to rely on. It states what this build does not yet do, and what to know before relying on it. It complements the [capability flows](capability-flows.md), [testing guide](testing.md) and [configuration guide](configuration.md).

A note on what is *not* here. This page used to list absent infrastructure — no queue, no broker, no worker runtime, no CI pipeline — as though each were a defect. They are design decisions, and describing them as shortfalls made the platform look unfinished in ways it is not. Those now sit under [Deliberate scope](#deliberate-scope), stated as what the system does. What remains below constrains whether the build is safe to rely on.

## Before relying on this build

| Limitation | Current behavior | Impact |
|---|---|---|
| Authorization is present but off by default | A capability layer classifies all 108 API operations and enforces them in one place, with bearer-token validation for both a local sign-in and an OIDC issuer. It ships behind `RBAC_ENABLED`, defaulting to false, so nothing is enforced until an operator turns it on and configures sign-in. The four policy-decision API operations — `POST /api/policy-decisions/{project_key}/case`, `POST /api/policy-decisions/{project_key}/case/light`, `POST /api/policy-decisions/{project_key}/policies`, and `GET /api/policy-decisions/{decision_id}` — are the exception: they require a valid authenticated principal (a bearer token, or a configured subscription key in `X-Policy-Subscription-Key`) and answer `401` without one even while the flag is off. The decision routes need to name who requested a receipt, and the policy route exposes approved records. That narrows those four routes; it does not enable enforcement anywhere else. | The build is not a security boundary as delivered. Configure sign-in and enable enforcement — see the checklist in [configuration](configuration.md) — before exposing it to untrusted users. Do not read "the decision API is authenticated" as "the product is authorized". |
| Stored case scenarios and caller guidance are kept indefinitely | Every call to the audited decision endpoint writes an append-only receipt holding the caller's `scenario` in clear, the normalised `additional_instructions` in clear, and the authenticated principal — because a receipt that does not show the question it answered is not evidence. Nothing prunes them. The repository has no scheduler, no worker runtime and no retention job, so there is no automated expiry, no configurable retention window and no deletion API for a receipt. | Free-form scenario text and caller guidance may contain personal data, and it persists for the life of the database. Retention, export and erasure are the operator's obligation and must be performed directly against the `policy_case_decisions` table. Treat this before putting the endpoint in front of end users who type their own prose. A retention worker was deliberately not invented here: it needs an operational scheduler this build does not have. |
| Pending decision receipts have no lease or automatic recovery | A decision reserves and commits a `pending` receipt before calling the model. The row has no heartbeat, owner lease, stale timeout, or startup reconciliation. `decision_in_progress` therefore means a pending row exists; it normally represents a live request, but a process failure can leave it there indefinitely. | Retry the same idempotency key while the original may still be live. If the process is known to have stopped and the row remains pending, operator intervention is required; no API repairs it. Starting a new key while liveness is uncertain can create a duplicate decision. |
| No packaged connector ships for agents, Copilot or workflow platforms | Integration is plain REST over HTTP with JSON, described by the API's own OpenAPI document. There is no native Copilot plugin, no Power Platform or Logic Apps custom connector package, no agent-framework tool package and no client SDK published from this repository. | Every integration authors and maintains its own mapping from the OpenAPI description to whatever its platform expects. Nothing is certified, marketplace-published or version-managed on your behalf. See [External consumption](external-consumption.md). |
| The subscription key is one shared credential with no lifecycle | A deployment may set `POLICY_SUBSCRIPTION_KEY` so a non-interactive caller can authenticate with a header instead of an OIDC token. It is exactly one key, mapping to one configured identity and role. It does not expire, cannot be revoked without restarting the API, and cannot be rotated without a window in which in-flight callers get `401`. Every caller holding it is the same principal in every receipt. It is unrelated to Azure API Management or Azure subscription keys — no gateway product is involved. | Two systems sharing the key are indistinguishable in the audit trail, and a leaked key is usable until an operator notices and restarts. Use it for a single trusted internal caller in a trusted environment; use an OIDC issuer when you need per-caller attribution or revocation. Never place it in a browser client: a shared credential in a page is a shared credential for everyone who loads it. The local `apps/consume-demo` playground does hold one in the browser, deliberately and only as a local demonstration against an operator-generated local key; it masks the value on screen and never exports it, but masking is a screenshot precaution and not a substitute for the rule above. |
| A case decision is model-mediated and may vary between calls | The audited endpoint runs the same retrieval-first project-case decider the product uses. The same scenario put twice to the same published version may legitimately produce different prose, a different explanation and a different `decision_hash`. `decision_hash` is an **integrity seal** over a documented subset of the receipt — it proves the stored content was not altered — not a determinism, reproducibility or replay guarantee. Reading a receipt back is **content-equivalent, not byte-identical**: every field carries the value it was written with and the hash verifies, but JSON key order may differ from the original response. | Do not build a control that expects two independent calls to seal identically, and do not compare a stored response string against a replayed one — compare `decision_hash`, or compare parsed values. Use an `Idempotency-Key` to get the original receipt back, or the deterministic `POST /api/evaluations` path with structured facts and its `result_hash` when reproducibility is what you need. No accuracy or benchmark claim is made about the answers. |
| Implicit concepts and heavily misspelled scenarios can miss the governing policy | Retrieval operates on the model-returned `processing_scenario`. The normaliser is instructed to return already-English input unchanged, but the service does not independently enforce byte equality and is not designed to correct spelling or infer an unstated policy concept. In live stress tests, “my bro owns one bidder” did not surface Conflict of Interest, “laptop gone” did not surface Lost or Stolen Equipment, and a heavily misspelled refresh question surfaced generic Replacement instead of Refresh Interval. Azure AI Search generative query rewriting could improve this class of recall, but it is preview-only, can synthesize incorrect restrictions, requires sending the scenario through another model, and is not enabled. | Inspect the sealed `language.processing_scenario` as well as the returned policies and citations. A wrong retained set can produce an incomplete non-answer even when the corpus contains a bearing rule. Integrations should state the policy subject explicitly where possible and must not treat `not_settled_by_rules` as proof the whole corpus is silent. Do not silently add domain-specific synonyms; a production query-expansion stage requires its own privacy, latency, recall, and false-expansion evaluation. |
| Contradictory facts and underspecified short asks can vary in status vocabulary | The decision plan fails safe when a proposed check is marked outcome-determinative or when that required flag is omitted, promoting it to `missing_required_facts`. A model can still express the same grounded non-final position as `not_settled_by_rules`, and in stress testing one directly contradictory faulty-device scenario produced one blocked run and one scoped answer. Both cited the bearing policy, but the machine status differed. | For automation, treat `answered` as actionable only within the exact scope stated in `decision` and `explanation`, and honor every `verification_requirement`. Use idempotency for retries, not a fresh independent call. When contradictory facts must deterministically block, validate them in the calling system or use the structured deterministic evaluation API rather than free-form case prose. |
| Irrelevant questions fail safely but can still consume significant model context | Six irrelevant live controls across AIS and HW produced no cited policy, no information answer, and no verdict. Retrieval and classification still ran, and one ambiguous football question consumed about 75,000 service-reported tokens before both tracks concluded that no rule bore. | Safety and cost are different gates. Do not expose the endpoint as a general-purpose assistant. Apply caller-side scope guidance and monitor `trace.token_usage`, `trace.stage_latency_ms`, and `outcome`; a safe `no_rule_bears` can still be an expensive request. |
| Translation is on the critical path and fails closed | Every decision reasons in English. A question in another language crosses in through a model call and its prose crosses back through another, under `case-language-v4`. Either crossing can fail: `scenario_translation_unavailable` and `scenario_translation_empty` mean no decision was attempted, `response_translation_unavailable` means a decision was made but could not be returned in the caller's language. All three are `503`. | The endpoint refuses rather than degrading — it will not adjudicate an empty rendering, and will not return English prose labelled as the caller's language. The dependency failure may be retryable, but the failed decision receipt spends its `Idempotency-Key`, so a later attempt needs a new key. A persistent failure means the platform cannot answer non-English questions at all until the model deployment recovers. |
| Original evidence is never translated, so a receipt is legitimately bilingual | Only prose the decision composed itself crosses back. Verbatim source sentences — `citations[].source.text` — stay in the language of the document, always, because a translated quotation is not a quotation. | A receipt can hold an Arabic quotation beneath an Arabic explanation reasoned in English. Client UIs must expect this and must not present a translated quote as the source. Readers who cannot read the source language get an explanation they can read and evidence they cannot verify unaided; that is the honest trade and no machine translation of the evidence is offered to paper over it. |
| The policy index must be current and quality-passed before a project can be retrieved from | Retrieval requires a `ready` manifest under `policy-english-projection-v1` with a recorded `passed` verdict under `policy-projection-quality-v1`. A project never indexed, mid-rebuild, indexed under a superseded projection, unvalidated, failed, or unavailable answers `503 index_projection_unavailable`. Migration `c1d4e8a92b73` adds the `projection_profile` column **nullable and deliberately un-backfilled** — a build that ran before projections existed really did not render anything, and NULL is that fact. | Carried projects need a rebuild or validation as their recorded state requires. This is an operator signal, not a transient fault. On a decision route it is discovered after receipt reservation and spends the original idempotency key, so the caller must use a new key after repair. Use `POST /api/policy-sets/{key}/policy-index/rebuild` and `POST /api/policy-sets/{key}/policy-index/validate`. |
| The projection-quality gate cannot detect a sibling-record substitution | The gate validates a built corpus against the records it was rendered from: deterministic checks on the document set (missing, unexpected, repeated, wrong version, wrong profile, empty text, orphaned child, per-record coverage against the rule threshold), then a per-document similarity floor of 0.60 under `policy-projection-quality-v1`. Any one document below the floor fails the whole corpus, and `unavailable` — a check that could not be made — is treated exactly as a failure. Measured against the embedding deployment over 27 synthetic pairs from several unrelated domains, faithful renderings score 0.71–0.84, unrelated records in the same document 0.25–0.48, and unrelated domains 0.18–0.24. **A fourth band the design did not anticipate — a *sibling record*, sharing subject, register and sentence shape and differing only in its identifiers, quantities and comparators — scores 0.58–0.84 and overlaps the faithful band by 0.13.** | The gate is decisive against **gross** substitution: a dropped record, a swapped output, a rendering of an unrelated record, an empty or wrong-profile document. It is **blind to the fine-grained case** — a rendering of a sibling record can score above a genuine rendering, so no single threshold separates them and raising the floor would block good corpora instead. The exposure is worst in exactly the records the gate matters most for: any schedule of many similar rows, where every row is a sibling of its neighbours. Treat a `passed` verdict as "the corpus is structurally sound and grossly faithful", not as "every row says what its source says". Closing it needs per-record alignment on the values records differ *by*, which is not built. Reproduce the measurement with `scripts/measure_projection_floor.py`. |
| Index rebuild is inline and uncached, and its cost scales with the corpus | A rebuild runs inside the HTTP request — both on the rebuild endpoint and best-effort after publish — because the corpus must be rendered and embedded before any of it can be indexed. Rendering is batched at 6 items or 3,000 characters per call and a batch never crosses a policy boundary, so the floor is one model call per published policy plus about one per six rule texts of any policy over 15 rules, all sequential, plus embeddings. Nothing is cached between builds: a rebuild re-renders the whole active version even if one policy changed. | Publishing a large corpus is slow and costs model calls in proportion to it, and so does every repair. Set generous client and reverse-proxy timeouts, and do not run two rebuilds against one project at once. A cache is not simply pending work — one keyed on less than the exact rendered contract would serve text from a superseded projection, which is what the profile stamp exists to prevent. |
| A rebuild has no rollback and no retry | Atomicity is a manifest state machine rather than a transaction, because Azure AI Search offers none: the manifest goes to `incomplete` first (and if that write is not acknowledged, nothing is written at all), documents are uploaded with acknowledgements counted by key, stale documents are swept only after that succeeds, and the manifest reaches `ready` last. A rendering failure for any single policy fails the whole build. | A failed rebuild leaves the previous documents in place and the project unmatchable — visible, rather than silently half-indexed. Recovery is to run the rebuild again: it is a pure function of the database, producing the same ids and overwriting in place. Nothing retries automatically, so a failed publish-time build stays failed until someone acts on it. |
| Live testing is English-only | The automated suite covers the language boundary with stubs and fixtures; the live end-to-end runs against a real corpus were made with English questions. The non-English path is exercised by unit tests, not by a live bilingual run. | Behaviour for a non-English question is asserted structurally rather than observed end to end at scale. Treat the first production use against a non-English corpus as a validation exercise, and read `language.boundary_state` and `output_rendering_state` on those receipts. |
| Local accounts hold plain-text passwords | `LOCAL_ACCOUNTS_ENABLED` reads credentials from a gitignored file so roles can be exercised before an identity provider is connected. The API refuses to start with it enabled while `ENVIRONMENT` is production. | A development convenience only. Use an OIDC issuer for anything else; the signing key beside it can mint tokens the API accepts, so it matters more than the passwords. |
| No tenant isolation | Policy data is not partitioned or authorized by organization. Roles are global, not per project. | Suitable only for a single trusted environment. |
| AI settings are not enforced at startup | The API starts with blank Azure settings. AI routes then return `503`, indexing returns `0`, and deterministic features keep working. | A deployment can look healthy while extraction is unavailable. Validate required settings before accepting traffic. |
| Documents are stored on the local filesystem | Uploads are written beside the API process. | Durability and backup are the operator's responsibility. |
| Indexing is best-effort | Clause indexing catches search failures, logs a warning and returns `clauses_search_indexed: 0` so upload still succeeds. | A document can exist in PostgreSQL and be absent from the grounding index. |
| Model output is validated after generation | Calls request JSON and Pydantic validates the result, rather than constraining generation to a schema. | Invalid output causes a retry or an explicit failure before anything is persisted. |
| The verbatim check is anchored to the batch, not the page | `verify_verbatim` compares a passage against the text the agent was shown, which is built from stored clauses. | It proves the model copied. It cannot detect text that ingestion stored wrongly, because both sides of the comparison come from the same stored clauses. See [What the verbatim check proves](ai-assistance.md#what-the-verbatim-check-proves). |
| Seven published passages carry an application label | The batch the agent reads includes labels this application adds, such as `(section: …)`. Because the verbatim check compares against that batch, a passage that copied a label was checked against the copy. Seven of the 280 rules in published v1 begin with `(section: Table of Violations and Penalties)`, which the source document does not contain. The extractor now strips these before verification, and a quality run reports any that remain as **blocking**. | A reader checking those seven citations against the document will not find the leading label; the rest of each passage is verbatim and the rules themselves are unaffected. Published versions are immutable, so this is corrected by the next version rather than in place — deliberately not forced, because re-extracting to fix seven citations rewrites records that are currently correct, and editing the stored records instead would break the audit trail the citations exist to support. |
| AI behavior is not verified against live services | Tests isolate the AI boundary; none call Azure OpenAI or Azure AI Search. | Retrieval relevance, index freshness and model behavior need validation in a real environment. |

## Deliberate scope

These are choices, not gaps. They are recorded so nobody re-derives them.

| Decision | How it works |
|---|---|
| Work runs in the request that starts it | Extraction and quality analysis are request-driven and can take minutes. The trade is visible: a restart interrupts the run, and progress is polled rather than streamed. |
| Lifecycle events are recorded, not broadcast | An outbox model persists what happened. Nothing consumes it yet, so a subscriber would be added against a table that already exists rather than a schema invented later. |
| Deployment is operator-triggered | The `azd`/Bicep kit is invoked by a person. Deployment automation belongs to the Azure phase, which is not finished. |
| Grounding is capability-specific | Ask AI and AI-proposed tests query the search index. Other calls ground on the source passage, selected rule, policy version or records their caller supplies. Do not assume every model call performs retrieval. See [How the AI is grounded](ai-assistance.md#how-the-ai-is-grounded). |
| Grounding is corpus-bounded | Answers come from uploaded documents and persisted policy data. That is the point: an answer traceable to a clause is worth more here than one drawn from the open web. |
| Retrieval calls the search service directly | There is no abstraction over it. Replacing the backend means changing the callers — accepted while there is one backend, because an interface with a single implementation states a generality nobody has tested. |
| One retrieval and one pair of statuses serve the whole case | An external case is read as up to two independent request types — what the policies *state*, and a *verdict* — but retrieval runs once from the whole question, both gathers read the same retained records, and the receipt has one information status and one verdict status. A live leave-plus-appraisal test retrieved and cited both policies in both runs, but one run answered both tracks while the other represented the combined request as one blocked verdict. The schema cannot express “leave blocked, appraisal answered” as independent per-topic outcomes. | Put independent business questions in separate calls when each needs its own status, idempotency key, receipt, or downstream action. A multi-topic call is appropriate only when one combined response is intended and the caller will read the full explanation and per-policy citations. |
| The retained set is bounded by size as well as by rank | Search ranks by relevance and knows nothing about record size, so a second pass admits whole policies in rank order while they fit the one-gather payload budget. A policy that would overflow is set aside as `outside_payload_budget`, disclosed and counted; later, smaller policies are still admitted. Nothing is trimmed and nothing is dropped silently — but a very large policy ranked below a smaller relevant one may go unread, and naming it as `provision_id` in single scope is the way to read it directly. |
| A policy over 15 rules is read rule by rule, not whole | Past `LARGE_POLICY_RULE_THRESHOLD` a provision is a schedule of independent rows rather than one governing statement, and a case touches a handful of them. Its rules are ranked against the question and at most `SELECTED_RULE_BUDGET` (15) are read **in total** — each selected rule's explicit `supersedes`/`related` context follows it in only into slots the selection left unused, never extending the budget, and anything that found no slot or no room is named in `context_rules_omitted`. `rule_selection` on the policy's `considered` entry names exactly which rules were read and how many were not, and no answer may present the policy as read whole. This replaced the failure it was written for: a question about annual leave retained the governing policy at rank 0 beside a 74-row `Table of Violations and Penalties` (provision key `e8326a491c60c8914b7932dd0f2607eb`, pages 21–27) at rank 3; their combined record measured 229,389 characters against a 200,000 budget and the gather refused everything, returning no answer for a question the corpus could answer. |
| Duplicate detection is conservative, so some real duplicates are read twice | Two policies — or two rules of one policy — are called duplicates only on an exact match of everything stored: every source sentence, condition, effect, type, mode, required fact, authority, scope, effective window, carve-out, and the resolved semantics of everything they supersede or relate to. A pair differing in any recorded respect is not collapsed, even when a reader would call them the same document. The measured case is one provision published twice where one copy records forty-two `related_rule_ids` and the other records none. That is deliberate: forgoing a collapse costs one slot in the retention budget, while a wrong collapse costs a reviewer the policy that governed their case. The consequence is that a corpus with sloppy near-duplicates still spends budget on them, and the real remedy is consolidating the corpus rather than guessing at retrieval time. |
| Diversity ordering can put a highly-ranked policy outside the budget | Because equality is conservative, candidates that *require* the same thing are additionally **ordered** so the highest-ranked of each normative group is offered before any second member (`policy_selection_order: relevance_then_normative_content_v1`). `policies_diversity_deferred` counts what that ordering cost — candidates that ranked inside the retention budget and were displaced out of it; a same-group member that ranked outside it anyway is not counted, so `0` means the ordering changed nothing. A deferred candidate is not a duplicate, is never labelled one, and keeps its own rank and score — but it carries the ordinary `outside_budget`, so that reason now means *did not place inside the retention budget* rather than *ranked below it*, and a rank-3 policy can sit outside a budget a rank-5 policy entered. The grouping withholds only `related_rule_ids`; anything that changes what a policy requires, supersession included, keeps the pair apart. Read the two ordering fields together to explain any such receipt. |
| Repeated source text is not treated as duplicate rule semantics | One sentence commonly states several obligations and is extracted into one rule each, so a single passage can back a permission, a prohibition, an obligation and a routing rule — collapsing on shared text would merge a rule that permits with one that forbids. Measured on the live corpora, 34 same-text groups covering 85 rules in one project and 71 groups covering 151 rules in another are all materially different under the full comparison. Only exactly identical rules are collapsed (`duplicate_rules_collapsed`, with `represented_rule_ids` naming unread copies of read rules); the rest are merely *ordered*, so the best rule of each distinct source passage is taken before a second rule from a passage already covered. The accepted cost is that a policy whose rows are genuinely redundant in wording still spends slots on them. |
| Rule selection is lexical, so it can miss a rule that bears in substance | Rules are ranked by an inverse document frequency computed over that policy's own rules — no wordlist, no second model call, and deterministic, which is what makes `selected_rule_ids` worth naming on a receipt. It matches on whole tokens, so a rule that bears on a question while sharing none of its vocabulary can be missed. The English projection removes the *cross-language* half of this: both sides of the comparison are now rendered under one contract, so an Arabic question is no longer matched against Arabic stored text by an English query. Two consequences remain visible rather than hidden: when nothing matches, `rule_selection.method` is `document_order` and a bounded sample is read instead of the policy being dropped; and a rule the caller holds no projection for scores zero on this rank and is counted in `rules_without_projection` rather than being scored against its stored text, which would put the cross-language match back. A stemmer for either language would tune the platform to one, which the domain-neutrality constraint forbids; the quantity rank and the rule index are the two other rankings fused with this one, and the honest bound is documented instead. |
| A policy larger than one grounded pass cannot be read whole | When a policy under the rule threshold exceeds the payload budget on its own, or when a large policy's *selected slice* does so, there is nothing smaller to fall back to: the record is kept, `size.oversize` is true and the track declines. Trimming would answer from part of a policy — or part of the relevant slice — while presenting as the whole, and dropping it would read as "no policy matched". Splitting such a policy at extraction time is the real remedy and is not implemented. |
| Intent detection is the server's, and has no request field | The two booleans in `asked` are read from the question by a classifier; there is no `needs` parameter, no mode flag and no UI control. A caller who could declare "this is a verdict question" could choose the shape of their own answer, which is the first thing caller guidance is forbidden to do — moving it to a different field would not make it a different thing. The reading is a model's, so it is reported with its reasoning, and an unreadable classification runs both tracks rather than dropping one. |
| The single-policy reviewer route still sorts a case into one kind | `POST /api/ai/policy-case/answer` — the reviewer's own per-policy surface — uses the older exclusive classifier and answers one branch. Only the project-case path (both the reviewer route and the audited external contract) reads a case as two tracks. The two are not merged because the per-policy route's shape is what its product surface reads today. |

## Workflow boundaries

| Capability | Where it stops |
|---|---|
| Policy tests | Proposed, accepted or rejected, run on demand and rerun after publication. No edit-in-place or hard delete, no bulk run, no schedule or trigger, no candidate simulation before publish. |
| Change management | Version comparison identifies added, removed and changed rules and can narrate them. It does not open a change request or approval workflow around the diff. |
| Quality and conflict analysis | Deterministic checks plus AI review. There is no independent contradiction engine and no automatic conflict resolution. |
| Rule relationships | Curated in the UI; older sample data may not populate them. Heuristic grouping is display assistance, not authoritative metadata. |
| Attestations | Campaigns and acknowledgements are stored. Reminders, escalation delivery, directory integration and automatic re-attestation are not implemented. |
| Ownership and RACI | Metadata only. Contacts are not validated and do not drive routing, notification or publish gates. |
| Exceptions | Requests have a stored lifecycle; no notification or external approval integration. |
| Exports | JSON, JSONL and CSV point-in-time downloads. No subscription or scheduled delivery. |
| Index maintenance | Each indexing write reconciles that document version's entries against the store, so re-extraction no longer leaves orphans searchable. A project's **policy** index additionally records what it last built, reports whether that still matches the active published version, and can be rebuilt on demand from the project Overview. What is still missing is anything scheduled or automatic: nothing sweeps for stale indexes, and a project that published before this existed stays unindexed until someone rebuilds it. |
| Decision receipts | A receipt is written on every audited external decision and read back by id. There is no list endpoint, no search across receipts, no export, and no deletion or redaction operation — the two published operations are the decide and the read-back. Receipts are readable by the caller who made the decision and by policy authors and administrators; everything else about their lifecycle is a database-level operator task. |
| Receipt schema versions | New decisions are written as `case_decision_v2`; receipts stored under `case_decision_v1` are still served as v1 and are never re-projected, because inventing the two `asked` booleans nobody classified for them would change a stored receipt's content after the fact. There is no migration of old receipt bodies and no down-projection of new ones: a client holding both must branch on `schema_version`. The database's `decision_status` column is kept and derived from the two tracks so existing operational queries keep working; the envelope in `response_json` is the authority. |

## Test coverage boundaries

The suite is strong around deterministic domain behavior and does not prove the deployed system:

- no database or Alembic migration tests
- no FastAPI integration tests
- no browser or end-to-end tests
- no automated frontend test runner
- no performance, load, penetration or dependency-security tests
- no tests against live Azure OpenAI or Azure AI Search resources

See [Testing and scripts](testing.md#current-coverage-gaps) for the verified inventory and commands.

## Structural debt

- **Routers issue SQL directly.** The repository layer exists so that query construction lives in one place, and it does not hold. **16** `session.execute` calls remain across **6** files under `api/`: `ai.py` (6), `documents.py` (5), `extraction.py` (1), `policy_sets.py` (2), `app.py` (1), `audit.py` (1).

It was 20 across 7. The largest single instance — 133 lines of aggregation behind `/review-facets` — moved into `infrastructure/persistence/review_facets.py`, which took `candidate_rules.py` from three direct queries to none. Retiring the extraction-stages read endpoint (`c5c06de`) removed one more, taking `extraction.py` from two calls to one — the query went because the surface it served went, not because it was rewritten.

This is containment, not a resolution. Each remaining call is small on its own, which is exactly why the pattern spread: no individual one looks like a decision. The cost is that this logic cannot be exercised without going through FastAPI, which is also why [no FastAPI integration tests](#test-coverage-boundaries) and this entry reinforce each other.

These counts are checked by `tests/unit/test_documented_sql_debt_is_current.py`, so the paragraph above cannot drift from the code without failing the suite.

- **Stored rule fingerprints are written and never read.** `domain/models.py` defines `content_fingerprint` and `anchor_fingerprint` on the candidate rule, and explains in a comment why they are stored rather than computed on read: the comparison is against runs that may be months old, and recomputing would silently re-interpret history if the definition ever changed.

The columns are written at insert time in `infrastructure/extraction/ai_extraction.py`. Nothing reads them. `diff_runs` (`infrastructure/projection/rule_delta.py`) recomputes both sides from the stored payloads, which is exactly the behaviour the comment gives a reason against. The guarantee is described, the storage that would provide it is paid for, and the consumer does not use it.

The code is left alone deliberately: recomputation is not currently wrong, and changing which side of this contradiction wins is a decision about historical comparability, not a cleanup. What is recorded here is that **the comment is the more persuasive of the two and is the one that is false** — it states a property, gives a reason, and reads as settled, while the behaviour it describes lives in another module that a reader has no cause to open.

- **Opening the largest policy renders every rule at once.** The Review and Policies rule card draws a policy's whole body — each rule with its name and inline detail — into the DOM in a single render, with no pagination or windowing, so the largest measured policy (72 rules) is the heaviest single render the build performs. In a queue that cost is bounded: a list draws collapsed heads, and a card's full body only when a reviewer opens it, not for every card scrolled past. The completeness test in `apps/web/src/nothingIsBehindAClick.test.tsx` draws that whole policy and carries a deliberately generous time budget, so a slow render is recorded as cost rather than tripping a stopwatch and reporting rules that are all present as missing.

- **A corrected extraction artefact survives in records already extracted.** `infrastructure/extraction/quantity_projection.py` used to borrow a comparison from a number-bearing predicate for a bare magnitude, so a plain quantity such as `"Received 3 doses"` could be projected as an instruction (`"You required to Received 3 doses"`). It now refuses that — a bare magnitude states what the quantity *is*, not a test, so it takes the `NO_COMPARISON` refusal — fixed at source in `18ca0e4`. The fix is forward-looking: rules extracted before it keep the wrong text until their document is re-extracted, so the artefact can still be read on a live record even though the defect is closed. Every surviving instance is in the `ais-employee-handbook` set — none in the GMU corpus — and some have already been approved or published, which is the part that catches people. Re-extraction alone does not clear an approved or published record: the corrected draft has to be reviewed, approved and republished before it replaces the live one — a governance act, not a technical one, and the only thing that rewrites an existing record.

- **The bulk-selection counter names no unit.** On both the Review and Policies panes the selection counter reads `N selected` without naming that the unit is policies. It is kept identical on the two surfaces on purpose; if it gains a unit it should gain one on both at once rather than let one side drift from the other.

- **The Overview omits two provenance facts it cannot honestly evidence.** A rule's sequence position in the document (`source_elements` is an element key such as `p1-E000004`, not an ordinal) and its ingestion time (`DocumentVersion.created_at` exists, but neither the Review nor Policies surface loads `SourceDocument`) are left out rather than approximated. Each is cheap to add once a caller loads the document record; neither is worth a request on its own.

## Documentation gaps

- **ADRs are cited but absent.** `ADR-0011` (XACML Obligation vs Advice) is referenced from five places in the code, and no such record was ever written. Decision records are kept on the workstation under `docs/internal/adr/` by convention rather than published, so a reader of this repository would not expect to find one here — but this record does not exist there either. The decision itself is real and is described in [Standards](standards.md); only the record is missing.
- **RFC 9457 is not implemented.** API errors use FastAPI's default `{"detail": ...}` rather than `application/problem+json`. Adopting it would be a small change; until then it is not claimed.
